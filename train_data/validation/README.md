# 虚拟女友验证集 (Validation Dataset)

本目录包含虚拟女友聊天数据集的验证集，用于模型训练后的验证和评估。

## 📁 文件说明

- `girlfriend_chat_validation_YYYYMMDD_HHMMSS.json` - 验证集数据文件
- `GENERATION_SUMMARY.md` - 生成报告详细信息
- `README.md` - 本说明文件

## 📊 数据集概览

- **数据条数**: 400 条
- **唯一场景**: 70 个
- **文件大小**: ~62 KB
- **生成时间**: 2024-11-23
- **编码格式**: UTF-8
- **数据格式**: JSON

## 🎯 数据集用途

1. **模型验证**: 用于训练过程中的验证集
2. **性能评估**: 评估模型在未见过数据上的表现
3. **过拟合检测**: 监控训练集和验证集的性能差异
4. **超参数调优**: 辅助选择最佳模型参数

## 📝 数据格式

每条数据包含三个字段：

```json
{
  "instruction": "对话场景/指令",
  "input": "用户输入（可为空）",
  "output": "女友的回复"
}
```

## ✅ 质量保证

验证集数据经过完整的质量控制流程：

- ✅ **唯一性**: 100% (无重复数据)
- ✅ **表情覆盖**: 100% (所有回复都包含表情)
- ✅ **语气一致性**: 100% (保持温柔体贴、俏皮可爱的女友风格)
- ✅ **长度控制**: 15-43 字符，平均 23.3 字符
- ✅ **场景多样性**: 涵盖 70 个不同场景

## 🔍 数据特征

### 场景分布 (18大类)
- 问候场景
- 情绪关怀
- 鼓励支持
- 生活照顾
- 健康关怀
- 天气关怀
- 日常聊天
- 赞美夸奖
- 兴趣爱好
- 美食分享
- 爱情表达
- 撒娇互动
- 工作学习
- 节日庆祝
- 冲突解决
- 未来规划
- 角色扮演
- 季节关怀

### 女友人设特征
- 温柔体贴 (Gentle and caring)
- 俏皮可爱 (Playful and cute)
- 阳光开朗 (Sunny and cheerful)
- 善解人意 (Understanding and supportive)
- 语气活泼 (包含"呀、啦、呢、哦"等语气词)
- 表情丰富 (每条回复都包含表情符号)

## 🔄 与训练集的关系

- **独立生成**: 验证集与训练集独立生成，避免数据泄漏
- **风格一致**: 使用相同的场景和质量控制标准
- **互补性**: 验证集覆盖所有主要场景类别
- **后续去重**: 需要与训练集进行交叉去重验证

## 📖 使用示例

### Python 读取

```python
import json

# 读取验证集
with open('train_data/validation/girlfriend_chat_validation_20251123_074751.json', 'r', encoding='utf-8') as f:
    validation_data = json.load(f)

print(f"验证集大小: {len(validation_data)}")

# 查看示例
for i in range(3):
    print(f"\n样本 {i+1}:")
    print(f"  场景: {validation_data[i]['instruction']}")
    print(f"  输入: {validation_data[i]['input']}")
    print(f"  输出: {validation_data[i]['output']}")
```

### 数据加载 (用于训练)

```python
from torch.utils.data import Dataset, DataLoader

class GirlfriendValidationDataset(Dataset):
    def __init__(self, json_file):
        with open(json_file, 'r', encoding='utf-8') as f:
            self.data = json.load(f)
    
    def __len__(self):
        return len(self.data)
    
    def __getitem__(self, idx):
        return self.data[idx]

# 创建验证集
validation_dataset = GirlfriendValidationDataset(
    'train_data/validation/girlfriend_chat_validation_20251123_074751.json'
)
validation_loader = DataLoader(validation_dataset, batch_size=8, shuffle=False)
```

## 🛠️ 生成方法

验证集使用以下命令生成：

```bash
python3 train_data/dataset/generate_girlfriend_dataset.py \
  --dataset-size 400 \
  --output-dir train_data/validation \
  --output-prefix girlfriend_chat_validation
```

## 📈 质量监控

定期检查验证集质量：

1. **唯一性检查**: 确保无重复数据
2. **表情覆盖率**: 确保所有回复包含表情
3. **长度分布**: 确保长度在合理范围内
4. **语气一致性**: 确保保持女友人设
5. **场景覆盖**: 确保各类场景均衡分布

## 🔗 相关文档

- [数据集文档](../dataset/README_DATASET.md) - 完整数据集说明
- [生成报告](./GENERATION_SUMMARY.md) - 详细的生成统计
- [项目README](../../README.md) - 项目总体说明

## 📞 联系与反馈

如发现数据质量问题或有改进建议，请提交 Issue 或 Pull Request。

---

**最后更新**: 2024-11-23  
**版本**: 1.0  
**维护者**: AI Dataset Team
